name: Migrate Page Views

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * 0' # Every week

jobs:
  main:
    name: Migrate Page Views
    runs-on: ubuntu-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Run npm install
        run: npm ci

      - name: Set up test environment file
        run: echo "DATABASE_URL=dummy" > .env && echo "GOOGLE_ANALYTICS_SERVICE_ACCOUNT=dummy" >> .env && echo "GOOGLE_ANALYTICS_PROPERTY_ID=dummy" >> .env

      - name: Run Migrate Page Views Script
        run: npm run migratePageViews
